# ───── OS ─────
.DS_Store
Thumbs.db

# ───── Editores / IDE ─────
.vscode/
.idea/
*.swp
*.swo
*~

# ───── Variables de entorno ─────
.env
.env.local
.env.*.local
*.env

# ───── Node / Vite (frontend) ─────
node_modules/
dist/
.vite/
.cache/
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ───── Python (backend) ─────
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
.venv/
venv/
env/
.python-version

# Tooling de tests / type checking (por si se agregan en la próxima iteración)
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
coverage.xml
.tox/

# ───── Docker ─────
# Volúmenes locales si se montan
postgres-data/

# ───── Logs ─────
*.log
logs/

# ───── Artefactos del proyecto ─────
# Bundles temporales del flujo Claude Design / handoffs
/tmp/
*.bin
*.tar.gz

# Build de docs (PDF se mantiene trackeado a propósito como entregable)
# palvi-construccion.pdf  ← intencionalmente NO ignorado

# ───── Secretos ─────
*.pem
*.key
secrets/
